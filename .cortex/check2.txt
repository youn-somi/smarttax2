JSON.stringify((() => { const out = []; const vw = document.documentElement.clientWidth; const walk = (el) => { const r = el.getBoundingClientRect(); if (r.right > vw + 2) { if (el.children.length < 12) { out.push({ tag: el.tagName, cls: String(el.className).slice(0, 60), right: Math.round(r.right), w: Math.round(r.width) }); } } for (const c of el.children) { walk(c); } }; walk(document.body); return { vw: vw, scrollW: document.documentElement.scrollWidth, bad: out.slice(0, 12) }; })())
