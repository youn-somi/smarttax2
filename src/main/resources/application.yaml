spring:
  application:
    name: smarttax

  datasource:
    url: jdbc:mysql://localhost:3306/smarttax
    username: root
    password: 1111
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true